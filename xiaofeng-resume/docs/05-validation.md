# V1.1 Beta 本轮验证记录

本轮日期：2026-09-14。旧版联调记录不作为本次证据。

## 已执行

- 31 项自动测试通过：API 入队与幂等、10 MB 文件元数据检查、非本人路径与未登录拒绝、资源额度、JD 重生成、评分卡确认及版本冲突、原文证据、敏感属性、解析、后台生成、调用日志、任务重试、并发名额、旧结果隔离、邀请码和 RLS、内部投递鉴权、前台按需轮询、TopK 重合率。
- 001～009 数据库迁移在 PGlite 中执行成功，使用 PostgreSQL RLS、事务与存储过程，而非 SQLite 替代 SQL 行为。
- TypeScript 检查通过。
- Next.js 生产构建通过。
- 三个 Netlify 函数的内部模块打包通过；这不是 Netlify 云端部署验证。
- ESLint 无错误，34 条动态 JSON / 数据库边界的显式 any 警告保留；已在 README 说明，并未声称零警告。
- pnpm 锁文件安装完成，指定依赖构建脚本执行成功。

测试入口：`pnpm test`、`pnpm typecheck`、`pnpm lint`、`pnpm build`。

## 未执行 / 不能由本地测试证明

- 真实 Supabase 项目迁移、邮箱验证与 Storage 直传。
- 真实 Netlify 后台/定时函数运行、关闭页面后的持续处理与生产配额。
- 真正跨进程并发压测。PGlite 验证存储过程行为，生产多实例锁竞争仍需云端复验。
- 真实 DeepSeek 付费调用、峰谷计费及模型质量。
- 浏览器完整视觉验收与真实 PDF 大文件网络上传；10 MB API 测试使用 Storage 元数据夹具。
- 5～20 位 HR、10～50 份/岗位的真实脱敏简历盲排与命中率评估。

所有模型响应为合成夹具，不应作为产品效果案例。部署后人工验收步骤见 NETLIFY-SUPABASE.md。
