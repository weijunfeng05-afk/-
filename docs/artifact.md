# MVP Word 文档生成约定

- 参考模板：C:/Users/admin/.codex/plugins/cache/openai-curated-remote/openai-templates/0.1.1/skills/artifact-template-system-design/assets/reference.docx。
- 沿用已选 System Design 模板；参考文件只读。SHA256 和完整包清单由生成器写入 QA 报告。
- 参考共 7 页、1 个节，已使用 Word 导出并逐页查看。Letter 8.5 × 11 英寸；上下边距 0.7 / 0.6201 英寸，左右 0.7 英寸，页眉页脚距离 0.5 英寸，首页不同。
- 保留封面两个 Title 段落、封面元信息表、正文 Heading 1 / Heading 3、正文段落和深蓝表头浅蓝数据行组件。正文段落使用参考第 22 段的 1.25 倍行距与 5.5 pt 段后距；标题及表格字体、颜色、单元格边距继承模板。
- 中文字符指定 Microsoft YaHei 回退字体，西文字体保持源设置；不更改 styles.xml、numbering.xml、主题、页眉和节属性。
- 中文行框更高，封面标题后减少四个空白占位段落，确保元信息表完整留在封面；表头显式保留白色粗体，避免段落属性覆盖原有文字颜色。
- 编辑位置：document.xml 封面文本及正文内容；footer XML 中组织名和文档类别；core.xml 标题与作者信息。其余包部件逐字节保持。
- 产品文档封面字段：版本、使用对象、日期；封面表为产品、目标、核心流程、交付范围。正文 4 页：范围与流程、页面设计、评分分类、验收与交付。
- 技术文档同样封面；正文 6 页：架构、处理流程、数据结构、接口契约、评分任务机制、部署与验证。
- 正文槽位允许按篇幅克隆源段落和表格模式；删除未使用的示例章节、占位图及其说明、示例脚注引用和示例链接，不增加无依据的需求。保留孤立的原始媒体和关系部件以维持包结构。
- 表格采用参考实际表格组件并按数据列数选择源表，保留表头、交替填色、自动行高及禁止拆行规则。必要时重设列宽以容纳中文标签。
- QA：两份 DOCX 的内容、关键需求、模板结构保留、无占位符测试通过；Word 导出 PDF 并通过 PDFium 生成所有页面 PNG，逐页检查文字、表格、分页和页脚。PDF/PNG 仅用于 QA，不交付。
- 环境限制：标准 render_docx.py 已尝试，因无 LibreOffice 失败；本机 Word COM 在沙箱外成功导出参考文件，最终文档采用同一备用路径。Word 只读打开，不回存修改 DOCX。
