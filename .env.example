# The startup script generates this key in .env automatically.
# Do not copy this placeholder as a real encryption key.
# RESUME_MASTER_KEY=<Fernet key generated by scripts/run.py>
# Optional process environment variables:
# RESUME_PORT=8000
# RESUME_DATA_DIR=<absolute local data directory>
